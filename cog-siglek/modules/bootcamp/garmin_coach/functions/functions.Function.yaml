name: Garmin Activities Extractor
externalId: garmin_activities_extractor
owner: Bootcamp Team
description: Extracts activities from Garmin Connect API and ingests into CDF (Events, Data Model, Time Series)
metadata:
  version: "1.0"
runtime: py311
functionPath: ./handler.py
# Required when CDF project uses DATA_MODELING_ONLY (stores function code as CogniteFile in this space)
space: {{ garmin_coach_space_name }}
# Do not declare secrets/envVars here with ${GARMIN_*} — CDF rejects deploy when those env vars are unset (null).
# After deploy: in Fusion → function → add secrets garmin-email, garmin-password, garmin-tokens and/or env GARMINTOKENS,
# or redeploy with a .env that defines them. See module README.
